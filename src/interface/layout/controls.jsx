import React from "react";

function controls() {
    return (
        <form className="controls_section">
            <fieldset>
                <legend>Control Panel</legend>
            </fieldset>
        </form>
    );
}

export default controls;
